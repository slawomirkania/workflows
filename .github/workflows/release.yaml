name: release
on:
  push:
    branches:
      - main
    paths-ignore:
      - ".github/**"
      - "README.md"
      - "version.sbt"

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Runt tests
        run: sleep 5
  release:
    needs: tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Release
        run: |
          featureName=$RANDOM
          touch $featureName
          git add $featureName
          git commit -m "New feature $featureName"
          git tag $featureName
          git push && git push --tags
